apiVersion: v1
kind: Service
metadata:
  name: {{ include "compute-allocation-service.fullname" . }}
  labels:
    {{- include "compute-allocation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "compute-allocation-service.selectorLabels" . | nindent 4 }} 