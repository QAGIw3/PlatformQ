apiVersion: v1
kind: Service
metadata:
  name: {{ include "collaboration-platform-service.fullname" . }}
  labels:
    {{- include "collaboration-platform-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "collaboration-platform-service.selectorLabels" . | nindent 4 }} 