apiVersion: v1
kind: Service
metadata:
  name: {{ include "state-management-service.fullname" . }}
  labels:
    {{- include "state-management-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "state-management-service.selectorLabels" . | nindent 4 }} 