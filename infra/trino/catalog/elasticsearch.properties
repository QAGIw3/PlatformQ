connector.name=elasticsearch
elasticsearch.host=elasticsearch
elasticsearch.port=9200
elasticsearch.default-schema-name=default
elasticsearch.scroll-size=5000
elasticsearch.scroll-timeout=1m
elasticsearch.request-timeout=10s
elasticsearch.max-retry-time=30s
elasticsearch.node-refresh-interval=1m

# Security (if enabled)
# elasticsearch.security=PASSWORD
# elasticsearch.auth.user=elastic
# elasticsearch.auth.password=changeme

# SSL/TLS (if enabled)
# elasticsearch.tls.enabled=true
# elasticsearch.tls.verify-hostname=true
# elasticsearch.tls.truststore-path=/etc/trino/elasticsearch.truststore
# elasticsearch.tls.truststore-password=changeit 