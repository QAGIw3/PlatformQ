apiVersion: v1
kind: Service
metadata:
  name: {{ include "federated-learning-service.fullname" . }}
  labels:
    {{- include "federated-learning-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "federated-learning-service.selectorLabels" . | nindent 4 }} 